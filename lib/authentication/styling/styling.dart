import 'package:flutter/material.dart';

const appBarTextStyle = TextStyle(
  fontWeight: FontWeight.bold,
  fontSize: 40,
  color: Colors.white,
);

const labelTextStyle = TextStyle(
  fontSize: 20,
  color: Colors.black,
);

RoundedRectangleBorder elevatedStyle = RoundedRectangleBorder(
borderRadius: BorderRadius.circular(12),
);

const buttonTextStyle = TextStyle(
  fontSize: 30,
  color: Colors.white,
);

const weightTextStyle = TextStyle(
  fontSize: 120,
  color: Colors.white,
);

const unitTextStyle = TextStyle(
  fontSize: 40,
  color: Color(0x80ffffff),
);

const focusedBorderStyle = OutlineInputBorder(
  borderSide: BorderSide(color: Colors.blueAccent, width: 2.0),
);

const enableBorderStyle = OutlineInputBorder(
  borderSide: BorderSide(color: Colors.black, width: 1.0),
);